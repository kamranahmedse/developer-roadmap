#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

   int n,a[n],sum=0,i;
   scanf("%d",&n);
 
   for(i=1;i<n;i++)
   {
    
     scanf("%d ",&a[n]);
 
   }  
   for(i=1;i<=n;i++)
   {
    sum=sum+a[n];
   } 
  
   printf("sum=%d",sum);
    return 0;
}