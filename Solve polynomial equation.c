#include<stdio.h>

int main()
{
    int x,a;
    printf("enter the value of x");
    scanf("%d",&x);
    a=4*x*x*x-5*x+9;
    printf("the answer of polynomial equation is %d",a);
    return 0;
}